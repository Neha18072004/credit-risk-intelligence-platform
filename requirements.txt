# ---------------------------------------------------------------------------
# Runtime dependencies (Python 3.11)
#
# This is what the application and the training pipeline need. Development
# extras -- notebook execution, tests, the optional hosted LLM SDKs -- live in
# requirements-dev.txt so they stay out of the deployed image.
#
# Two packages were removed after checking: plotly (188MB) and seaborn were
# listed but referenced nowhere. All charting goes through matplotlib and the
# shared theme in src/utils/viz.py.
# ---------------------------------------------------------------------------

# --- core data stack ---
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1

# --- modelling ---
scikit-learn==1.5.2
lightgbm==4.5.0          # the selected model
catboost==1.2.7          # bake-off candidate; see requirements-serve.txt to omit
shap==0.46.0
joblib==1.4.2

# --- visualisation ---
matplotlib==3.9.2

# --- database ---
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# --- NL -> SQL safety ---
sqlglot==25.24.5

# --- config and app ---
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
streamlit==1.39.0
